server:
  port: 8080 # 项目启动端口

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 注意修改为你的数据库名、用户名和密码
    url: jdbc:mysql://localhost:3306/erp_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: my$dianshang@260105

# MyBatis-Plus 配置，用于在控制台打印 SQL 语句，方便调试
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
